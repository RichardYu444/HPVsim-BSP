import hpvsim as hpv

#initial parameters according to table
#pars = dict(
    ## initial agent settings 
 #   n_agents = 1000,
  #  location = 'England'
   # network = 'default'
    #init_hpv_prev =
    #init_hpv_dist =
    #start = 2000
   # n_years = 50
    #burnin =
    #dt = #(simulation time steps)
    #use_migration =
    ## cluster settings
    #n_clusters =
    #cluster_rel_sizes =
    #add_mixing = 
#)
hpv.data.show_locations()
##multisim for some uncertainty testing